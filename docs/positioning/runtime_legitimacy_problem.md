# Runtime Legitimacy Problem

The core infrastructure problem is temporal:
authorization can persist while runtime reality changes.

When execution occurs later, historical authorization can become misaligned with present runtime state.
