# Why Existing Systems Fail

Most approval-first systems assume the runtime remains effectively stable between approval and execution.

Under ordinary runtime evolution, this assumption fails and execution legitimacy becomes uncertain.
